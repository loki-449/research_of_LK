# lib 公共库配置（物理默认值，一般无需修改）

# 赝势库（make_potcar / run_*_batch 的 --pbe-lib 默认值）
export PBE_LIB=/home/test1/hhy/basic/psudopotential/PAW-GGA-PBE

# 计算工作根（所有入口脚本 --work-root 的 fallback）
export VASP_WORK_ROOT=/home/test1/hhy/calculation/vasp_work
